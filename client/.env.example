# Client environment variables (example)
# Copy to client/.env.local for local development and update values

# Public API URL the frontend will call (include /api if your backend exposes API under /api)
NEXT_PUBLIC_API_URL=http://localhost:4000/api

# Any other public environment variables (prefix with NEXT_PUBLIC_)
# NEXT_PUBLIC_ANALYTICS_ID=
